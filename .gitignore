# ROMs must never be pushed (copyright) — keep them local only
roms/

# Test artifacts and dependencies (generated, not needed in the repo)
node_modules/
test/*.png
test/ingame/
test/fixtures/
package-lock.json

# test scratch
test/cl/
test/*.log
test/_*.mjs
test/_commitmsg.txt

# online server runtime data (lifetime scores)
server/scores.json
